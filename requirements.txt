# Modules that you must have installed to run this program
# run this command in the directory terminal
# pip install -r requirements.txt (windows/mac)
# pip3 install -r requirements.txt (linux)
pandas
matplotlib